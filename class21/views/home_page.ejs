<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Airbnb Clone</title>
  <link rel="stylesheet" href="output.css" />
</head>
<body class="bg-gray-100 font-sans">
  <%- include('./Partials/Header.ejs') %>

  <div class="min-h-screen flex items-center justify-center">
    <div class="w-[90%] max-w-6xl p-6 bg-white shadow-xl rounded-lg">
      <h1 class="text-4xl font-extrabold text-red-500 text-center mb-8">
        Welcome to Airbnb Web Clone
      </h1>

      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
        <% HomeData.forEach((data) => { %>
          <div class="border border-gray-200 p-6 rounded-xl shadow-sm hover:shadow-lg transition duration-300 bg-gray-50">
            <div class="h-48 overflow-hidden rounded-xl mb-4">
              <img src="<%= data.img %>" alt="Home image" class="w-[20px] h-[20px]>
            </div>
            <h2 class="text-xl font-semibold text-gray-800 mb-2">
              <%= data.name %>
            </h2>
            <p class="text-gray-600 mb-1">
              <span class="font-medium text-gray-700">Email:</span> <%= data.email %>
            </p>
            <p class="text-gray-600">
              <span class="font-medium text-gray-700">Home:</span> <%= data.home %>
            </p>
          </div><br> 
        <% }) %>
      </div>
<br>
      <div class="mt-[20px] text-center">
        <a href="/add_home" class="inline-block px-8 py-3 w-full bg-red-500 text-white font-semibold rounded-full hover:bg-red-600 transition duration-300 shadow-md">
          ➕ Add  Home
        </a>
      </div>
    </div>
  </div>

</body>
</html>
